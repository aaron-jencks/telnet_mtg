#include "login.h"
#include <stdio.h>

interaction_response_t login_window() {
    interaction_response_t response;
    response.keep = 1;
    response.window = NULL;

    printf("Welcome to the TelNet MTG battle arena!\nPlease login\n");
    while (1) {
        char* 
        printf("Username: ")

    }

    return response;
}